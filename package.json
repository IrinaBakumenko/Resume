{
  "name": "resume",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "parcel build src/index.html",
    "start": "parcel src/index.html --open"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.28.0"
  },
  "dependencies": {
    "css": "^3.0.0"
  }
}
