{
  "name": "resume",
  "version": "1.0.0",
  "homepage": "https://ibakum.github.io/Resume/",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "parcel build src/index.html",
    "start": "parcel src/index.html --open",
    "eject": "react-scripts eject",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.28.0"
  },
  "dependencies": {
    "css": "^3.0.0"
  }
}
